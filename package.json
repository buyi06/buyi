{
  "name": "libretv-cloudflare",
  "private": true,
  "workspaces": [
    "apps/worker",
    "apps/web",
    "packages/drizzle",
    "packages/shared"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "deploy": "pnpm --filter @libretv/worker deploy"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}